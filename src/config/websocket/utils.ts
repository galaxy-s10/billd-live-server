export const readFileSync = () => {
  // const chunkDir = path.resolve(__dirname, './chunk/');
  // const urlTxt = path.resolve(__dirname, './url-1.txt');
  // let str = '';
  // fs.readdirSync(chunkDir).forEach((file: string) => {
  //   const isWebm = getFileExt(file) === 'webm';
  //   if (!isWebm) return;
  //   str += `file '${path.join(chunkDir, file)}'\n`;
  // });
  // fs.writeFileSync(urlTxt, str);
};
